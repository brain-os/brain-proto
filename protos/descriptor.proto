syntax = "proto3";

package com.jio.brain;

option java_multiple_files = true;

message BrainDescriptor {
  string vertical = 1;			// telecom, retail, common
  string type = 2;			// entity, event, attribute
  repeated string name = 3;
  string key = 4;			// created from a logic
}
